defmodule JanusEx.RoomTest do
  use JanusEx.RoomCase

  # @registry JanusEx.Room.Registry
  # alias JanusEx.Room

  describe "JanusEx.Room" do
    test "when create room", %{room_name: _room_name} do
      assert true
    end

    test "when leave room" do
      assert true
    end

    test "when list rooms" do
      assert true
    end

    test "when check room exist?" do
      assert true
    end

    test "when user join room" do
      assert true
    end
  end
end
